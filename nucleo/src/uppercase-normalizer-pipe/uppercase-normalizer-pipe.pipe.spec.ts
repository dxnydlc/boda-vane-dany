import { UppercaseNormalizerPipePipe } from './uppercase-normalizer-pipe.pipe';

describe('UppercaseNormalizerPipePipe', () => {
  it('should be defined', () => {
    expect(new UppercaseNormalizerPipePipe()).toBeDefined();
  });
});
